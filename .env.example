VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
# Optional for local dev when using an external API server
# If omitted, frontend will call same-origin /api in production
VITE_VERIFY_API_BASE_URL=http://localhost:5000
